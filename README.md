# Composer Package Updater

A CLI tool to audit and update Composer packages.

## Installation

```bash
git clone https://github.com/kristaps777/composer-package-updater.git
cd composer-package-updater
npm install
```

## Configuration
Make sure `PROJECTS_ROOT` is set in .env

## Usage

```bash
npm run start
```